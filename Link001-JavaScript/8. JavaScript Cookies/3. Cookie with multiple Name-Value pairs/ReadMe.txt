In JavaScript, a cookie can contain only a single name-value pair
In JavaScript, a cookie can contain only a single name-value pair
    1. Serialize the custom object in a JSON string, parse it and then store in a cookie.
    2. For each name-value pair, use a separate cookie