//**The JavaScript date getHours() method returns the hour for the specified date on the basis of local time. */
var $hour = new Date()


var $stringDate = $hour.toString() // toDateString()
console.log($hour.getHours())
console.log($stringDate)