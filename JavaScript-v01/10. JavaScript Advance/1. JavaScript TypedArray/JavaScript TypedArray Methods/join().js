/**
 * The JavaScript join() method is used to join all elements of an Array into a string
 * The elements will separated by a specified separator. The default separator is comma(,).
 * Syntax: Array.join(separator)
 * Parameters: 
 * It is optional, it can be either used as a parameter or not its default value is (,).
 * Return: It returns the String which contain the collection of array 's elements
 * 
 * 
 * 
 * 
 * 
 * 
 */

//example 1
var items = ['audi', 'bmw', 'ferrari', 'bently', 'porsche', 'chevrolet']
console.log(items.join('-')) // audi-bmw-ferrari-bently-porsche-chevrolet